<Window x:Class="windows_automation_utility.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:windows_automation_utility"
        mc:Ignorable="d"
        Title="Windows Automation Utility" Height="650" Width="1100"
        Background="#F0F0F0">
    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="Settings" Click="SettingsMenuItem_Click"/>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

        <!-- Left Panel: Application Selection -->
        <GroupBox Header="Select Application" Grid.Column="0" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListBox x:Name="AppListBox" Grid.Row="0" Margin="5" SelectionChanged="AppListBox_SelectionChanged" />
                <Button x:Name="RefreshButton" Content="Refresh List" Grid.Row="1" Click="RefreshButton_Click" />
                <TextBlock Text="Or enter custom EXE path:" Grid.Row="2" Margin="5" FontWeight="Bold"/>
                <TextBox x:Name="CustomExeTextBox" Grid.Row="3" TextChanged="CustomExeTextBox_TextChanged" />
            </Grid>
        </GroupBox>

        <!-- Right Panel: API Configuration -->
        <GroupBox Header="API Configuration" Grid.Column="1" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="API URL:" Grid.Column="0" Grid.Row="0" Margin="5" VerticalAlignment="Center" FontWeight="Bold"/>
                <TextBox x:Name="UrlTextBox" Grid.Column="1" Grid.Row="0" Text="https://unifi-controller.local:8443/api/" />
                <TextBlock Text="HTTP Method:" Grid.Column="0" Grid.Row="1" Margin="5" VerticalAlignment="Center" FontWeight="Bold"/>
                <ComboBox x:Name="MethodComboBox" Grid.Column="1" Grid.Row="1" SelectedIndex="1">
                    <ComboBoxItem>GET</ComboBoxItem>
                    <ComboBoxItem>POST</ComboBoxItem>
                    <ComboBoxItem>PUT</ComboBoxItem>
                    <ComboBoxItem>DELETE</ComboBoxItem>
                </ComboBox>
                <TextBlock Text="API Key:" Grid.Column="0" Grid.Row="2" Margin="5" VerticalAlignment="Center" FontWeight="Bold"/>
                <TextBox x:Name="ApiKeyTextBox" Grid.Column="1" Grid.Row="2" />
                <TextBlock Text="Headers:" Grid.Column="0" Grid.Row="3" Margin="5" VerticalAlignment="Top" FontWeight="Bold"/>
                <TextBox x:Name="HeadersTextBox" Grid.Column="1" Grid.Row="3" Text="Content-Type: application/json" AcceptsReturn="True" Height="60" />
                <TextBlock Text="Request Body:" Grid.Column="0" Grid.Row="4" Margin="5" VerticalAlignment="Top" FontWeight="Bold"/>
                <TextBox x:Name="BodyTextBox" Grid.Column="1" Grid.Row="4" AcceptsReturn="True" />
                <Button x:Name="TestApiButton" Content="Test API" Grid.ColumnSpan="2" Grid.Row="5" Margin="5" Click="TestApiButton_Click" VerticalAlignment="Top" />
            </Grid>
        </GroupBox>

        <!-- Bottom Panel: Control and Status -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <Button x:Name="StartMonitoringButton" Content="Start Monitoring" Click="StartMonitoringButton_Click" />
            <Button x:Name="StopMonitoringButton" Content="Stop Monitoring" Click="StopMonitoringButton_Click" IsEnabled="False" />
            <TextBlock x:Name="StatusTextBlock" Margin="10,0" VerticalAlignment="Center" FontWeight="Bold" />
        </StackPanel>
        </Grid>
    </Grid>
</Window>
